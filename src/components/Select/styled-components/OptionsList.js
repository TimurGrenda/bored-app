import styled from 'styled-components';

const OptionsList = styled.ul`
  position: absolute;
  height: 100px;
  list-style: none;
  overflow-y: scroll;
  padding-left: 0;
`;

export default OptionsList;
